/*Empty*/
{

}

/*Simple*/
{
  "Listen": "localhost:8081"
}

/*Full*/
{
  "Environment": 0,
  "Listen": "localhost:8081",
  "Modules": {
    "Collab": {"DB": {"Type": "mysql", "DSN": "user:pass@tcp(localhost:3306)/passport?charset=utf8"}},
    "Passport": {"DB": {"Type": "mysql", "DSN": "user:pass@tcp(localhost:3306)/passport?charset=utf8"}},
    "Public": {"Name": "Public", "Path": "/", "Root": "{WorkingDir}/public"}
  }
}
